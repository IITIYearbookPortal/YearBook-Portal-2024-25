import React from 'react'

// to Anish
// send a get request on backend to /memories/get-pending-request
// youll get a list (currently only 2) of pending memories with their content and ids
// may show them in a card or smth and add two btns to accept, or reject(delete)
// send patch request to /memories/accept/:memoryId to accept on clicking btn 
// send delete request to /memories/delete/:memoryId to delete the memory on clicking btn
// refresh page on each click or btn to get the updated list of request

const VerifyMemories = () => {
  return (
    <div>VerifyMemories</div>
  )
}

export default VerifyMemories